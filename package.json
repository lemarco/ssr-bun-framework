{
  "name": "ssr-framework",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun --watch run ./index.ts ",
    "build:server": "bun build ./index.ts --target=bun --outfile=server.js",
    "clean:server": "rm -rf ./server.js",
    "build": "bun run build:server"
  },
  "dependencies": {
    "@komefumi/html-template-tag": "^0.0.4",
    "@types/react": "^18.2.48",
    "react": "^18.2.0",
    "terser": "^5.27.0",
    "vite": "^5.0.12"
  }
}